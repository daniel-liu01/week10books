import styles from "./InfoModal.module.css";

function InfoModal({ type }) {
    return;
    {
        type === "badge" && ()
    
    }  {
    type === "income" && ()
    
    }
}

export default InfoModal;
